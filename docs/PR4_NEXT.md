# Next implementation slice

After this auth/tenancy foundation is deployed and verified, implement the first revenue path:

`catalog item -> server-created checkout -> Square payment -> verified/idempotent webhook -> payment record -> entitlement -> service order -> portal history`

Use Social Presence Setup ($497 one-time) first, then Social Presence Management ($297/month) for recurring subscriptions.
